# 킥복싱 선수 포트폴리오 사이트

프로 킥복싱 선수의 경기·훈련 영상과 사진을 정리해 보여주는 정적 웹사이트.
서버와 데이터베이스 없이 운영하며, 고정 비용을 최소화하는 것을 전제로 설계한다.

## 문서

세부 내용은 아래 문서를 따른다.

- 서비스 기획: @docs/product.md
- 개발 가이드: @docs/development.md
- 작업 기록과 인계: @docs/worklog.md

## 작업 규칙

### Node 실행

로컬 머신에 Node.js가 없다. `node`, `npm`, `npx`, `pnpm`, `yarn` 을 로컬에서 직접 실행하지 않는다.
모든 Node 작업은 Docker 컨테이너 안에서 실행한다.

```
docker compose -f docker/compose.yml run --rm app <명령>
```

### 언어

모든 응답, 주석, 커밋 메시지, 문서는 한국어로 작성한다.

### 작업 순서

구현 전에 계획을 세우고 승인을 받는다. 문서에 없는 기능을 임의로 추가하지 않는다.
