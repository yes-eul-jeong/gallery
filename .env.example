# Cloudflare R2
# 계정 ID는 dash.cloudflare.com → R2 → Overview 우측에 표시된다
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=kickbox-media

# 미디어 서명 키
# openssl rand -hex 32 으로 생성한다
# Worker 시크릿에도 같은 값을 등록한다
MEDIA_SIGN_SECRET=

# Worker 도메인 (사이트 빌드에 포함되므로 PUBLIC_ 접두사)
PUBLIC_MEDIA_BASE=
